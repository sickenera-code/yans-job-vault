# Yan's job vault — Codex 项目入口

本目录是 Obsidian 秋招求职仓库。执行任务前读取根目录 `CLAUDE.md`，其中的简历事实边界、数据标记、岗位同名目录、资料双链和语义化媒体命名规则同样适用于 Codex。遵循更高优先级的运行时规则。

仓库结构说明见 `使用说明.md`。不要假设 Codex 会自动加载 `.claude/CLAUDE.md`。需要初始化或补齐目录时，读取已安装的 `yans-job-vault/SKILL.md`，使用它的脚本；脚本不在当前仓库内时定位实际 skill 目录，不能硬编码他人机器路径。
