---
title: 投递记录数据
tags:
  - 求职
  - 投递记录
---

# 投递记录数据

> [!warning] 自动维护文件
> 此文件由 [[投递进度]] 自动读写。请勿修改下面的数据标记或 JSON 结构；请将本文件与仓库一起同步或备份。
>
> **字段说明**：每条投递记录支持以下字段 —— `id`（唯一字符串）、`company`（企业名称）、`type`（企业类型）、`position`（岗位名称）、`date`（投递日期）、`link`（岗位链接，可选，留空表示无链接）、`stages`（阶段列表）、`terminated`（是否终止）、`offer`（是否获得 Offer）、`notePath`（同步笔记路径）、`folderPath`（同名资料文件夹）。缺少 `link` 字段的旧记录会被视为无链接，渲染时自动降级。

<!-- JOB_APPLICATIONS_DATA_START -->
```json
[]
```
<!-- JOB_APPLICATIONS_DATA_END -->
