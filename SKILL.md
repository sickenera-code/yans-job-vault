---
name: yans-job-vault
metadata:
  author: "aheart Yan"
  email: "953800971@qq.com"
  version: "1.0.0"
description: 从用户简历部署或维护 Obsidian 秋招求职仓库：保留现成投递、关注与从夯到拉评级页面，生成简历深挖、经历目录、岗位笔记及同名资料文件夹，并提供 Claudian/Claude Code/Codex 规则。适用于“搭建求职仓库”“用简历初始化秋招系统”及该仓库的日常维护；单独润色一句简历不必初始化仓库。
---

# Yan's job vault

作者 / 项目创作者：**aheart Yan**  
联系邮箱：**953800971@qq.com**  
Copyright © 2026 aheart Yan.

技能标识：`yans-job-vault`。项目署名见 [NOTICE.md](NOTICE.md)，许可见 [LICENSE](LICENSE)，已有方法来源见 [来源说明](references/provenance.md)。部署时保留使用说明中的项目署名；用户简历、经历和个人笔记不属于项目作者署名范围。

把用户的真实简历部署成可持续积累资料的 Obsidian 仓库。交付实际文件，不只给目录方案。页面模板已去掉个人投递数据，`公考时间.md` 按提供者要求原样保留。

## 判断当前任务

- **首次部署**：按下文完整执行。读取 [仓库契约](references/vault-contract.md)、[简历深挖流程](references/resume-deep-dive.md)。
- **补齐或维护现有仓库**：先读目标仓库 `CLAUDE.md`、`AGENTS.md`、简历和目标记录。按仓库契约修改相关文件；不重做与请求无关的页面或分析。
- **只制作模板 / skill 包**：不要把当前候选人的简历或投递记录复制进可分享的 skill。简历分析是未来调用时生成的产物。

## 首次部署

1. 确定简历输入和目标目录。默认在当前目录新建 `秋招求职仓库/`；用户明确当前目录就是目标时使用当前目录。只询问缺失且无法推断的必要信息。用户已提供的岗位/行业不重复询问；目标不明时标为“待确认”，仍可完成结构和通用分析。
2. 以本 `SKILL.md` 所在目录为技能根目录，运行：
   ```bash
   python3 "<技能目录>/scripts/vault.py" init --vault "<目标仓库>" --year 2026
   ```
   年份使用用户指定秋招年份；未指定用当前年份。脚本只创建缺失文件，打印保留项，不覆盖现有页面、规则、配置或数据。已有规则时由 Agent 把必要规则合并进原文，保留用户约定。不要复制其他仓库的 `.obsidian`、插件凭据、聊天记录或符号链接。
3. 按 [简历输入处理](references/resume-input.md) 读取 PDF、Word 或文本。将原件语义化命名后保存在 `Resource/Resumes/`，生成忠实的 `00-我的简历.md`，保留原文、来源定位和未识别片段。不要把优化稿冒充原文。
4. 先完成全景诊断，再对实际经历的每条实质陈述完成五维深挖，交付 `00-我的简历-深挖分析.md`。内置的 [resume-deep-dive 流程](references/resume-deep-dive.md) 和 [输出模板](references/resume-output-templates.md) 足以执行，无需另装外部 skill。首次部署默认完整深挖；可分批写文件，但不能只写前几条就声称完成。目标推断和待核实事实须明确标记。
5. 按真实简历生成模块清单 JSON，参照仓库契约中的 schema。教育、实习、工作、项目、校园实践、竞赛及独立技能模块按实际内容拆分；不能沿用模板提供者的学校、企业和专业。运行：
   ```bash
   python3 "<技能目录>/scripts/vault.py" decompose --vault "<目标仓库>" --profile "<模块清单.json>"
   ```
   清单最终存入 `Resource/简历结构.json`。每段独立经历有同名模块文件夹与概览，原文和深挖报告互链；把已生成的针对性追问和待补证据链接进概览。不要凭空创建不存在的实习。
6. 检查 `CLAUDE.md`（Claude/Claudian 主规则）、`.claude/CLAUDE.md`（入口指引）、`AGENTS.md`（Codex 入口）已落盘。模板中的 `使用说明.md` 解释 Obsidian/Dataview 和 Claudian 的运行步骤。仓库只是本地部署，不包含账户配置和插件二进制；按实际环境检查插件，不宣称已自动安装。
7. 运行 `vault.py check --vault "<目标仓库>" --complete`，再人工检查：深挖内容覆盖全部实质经历、原文可追溯、没有虚构指标、链接有效、两个数据块有效。可用 Obsidian 时验证页面新增一条测试记录后立即清理测试数据和测试笔记；不能操作 Obsidian 则明确实际渲染尚未验证。

## 岗位联动与日常维护

- 页面新增投递会创建 `已投递岗位/NN-公司 岗位.md`、同名文件夹和 `00-资料索引.md`。加载页面或导入记录会补齐缺失资料目录；现有笔记内容不覆盖。
- Agent 代记投递时，读取仓库契约，准备 JSON 后运行 `vault.py add-job --vault "<仓库>" --record "<记录.json>"`。保留稳定 id，用于重试去重。这是**记录已投递事实**，不是代用户去招聘网站提交申请。
- 旧记录缺目录时运行 `vault.py sync-jobs --vault "<仓库>"`。编号取笔记与目录现有最大值 + 1，不复用删掉记录留下的编号。已保存路径优先，重命名需同步两种路径和链接。
- 页面数据唯一来源是 `Resource/投递记录数据.md` 与 `Resource/秋招关注数据.md` 的带标记 JSON 数组。修改保留其他正文和未知字段；格式损坏先修复，不能以空数组重置。
- 同一时间使用一个写入入口；Agent 写入前关闭或停止操作相应交互页面，完成后重新打开页面刷新。跨设备同步冲突先比较差异，不以旧内存快照覆盖新文件。
- 媒体统一按 `Resource/Images/<主题>-<序号>-<内容描述>.<扩展名>` 命名，就近嵌入；原始简历放 `Resource/Resumes/`，其他附件放 `Resource/Attachments/`。

## 交付边界

不把仅有空标题的文件称为完成的简历深挖。没有用户简历时先搭建可做的结构，明确简历产物待输入后完成。扫描件或受损文件读不出时保留状态并说明缺失，不捏造经历。分析中的面试推荐回答只能使用已知事实，未知的 S/T/A/R 要素标为待补充。

公考页是提供者保存的 2026.1—2027.6 时间轴；原样保留不代表已核实当前公告。不要自行更新该模板；用户要求当前报考信息时再核对官方公告。

## 安装与调用

本文件夹可独立复制，不能只复制 `SKILL.md`。Claude Code / Claudian 的 Claude 模式可放入 `~/.claude/skills/yans-job-vault/` 或目标仓库 `.claude/skills/yans-job-vault/`；Codex 可放到当前环境已经使用的技能搜索目录（常见为 `~/.agents/skills/` 或 `~/.codex/skills/`）。直接提供本 `SKILL.md` 的路径也可让 Agent 读取执行，不依赖自动发现。

调用示例：“使用 yans-job-vault，读取这份简历，在指定目录搭建 Obsidian 秋招仓库，并完成全部简历深挖和经历拆分。”

## 岗位评级板

部署时包含 `已投递岗位/00-从夯到拉.md` 及初始为空的 `Resource/岗位评级数据.md`，保留五档拖拽评级、候补池、清除评级和双击打开笔记。评级依据用户偏好，不自动替用户决定。按岗位笔记路径关联，重命名需同步评级路径；清除评级不删除岗位。初始化脚本会复制这两个文件，已有评级数据不覆盖。
